import React from 'react';

const Footer = (props) => (
  <footer className="footer">
    <div className="container">
      <p className="text-muted pull-left">{'Copyright by FlashTrans'}</p>
      <p className="text-muted pull-right">{'Build with <3 by mozartilize'}</p>
    </div>
  </footer>
)

export default Footer;
